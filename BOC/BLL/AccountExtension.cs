using BOC.Models;
using CSharp.Functional.Constructs;
using static CSharp.Functional.Extensions.OptionExtension;

namespace BOC.BLL
{
    public static class AccountExtension
    {
        //public static Option<AccountState> Debit(this AccountState acc , decimal amount) =>
        //    acc.Balance < amount ? None : new AccountState() { Balance = (acc.Balance - amount) };
        
    }
}
